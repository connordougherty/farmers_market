<section class="hero is-primary is-medium" id="banner-slide">
  <div class="hero-body">
	  <div class="container">
		  <div class="columns">
		  	<div class="column is-offset-3">
			  	<h1 class="title is-1">
		        <%= @storefront.name %>
		        <% if can? :manage, Storefront, :user_id => current_user.id %>
	        		<%= link_to 'Edit Storefront', edit_storefront_path(@storefront), class: "button is-white is-outlined pull-right" %>
	        	<% end %>  
		      </h1>
	      </div>
      </div>
	  </div>
	</div>
</section>

<div class="container" id="circle-container">
	<div id="circle">
	</div>
</div>