<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'The Farmer\'s Market', root_path, class: 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
      </ul>
      <ul class="nav navbar-nav navbar-right">
      <li><%= link_to 'Cart', view_cart_path %></li>
      <% if user_signed_in? %>
        <li><p class="navbar-text">Logged in as 
        <% if current_user.seller? %>
          <%= link_to current_user.email, current_user.storefront %>
        <% else %>
          <%= link_to current_user.email, current_user.profile %>
        <% end %>
          </p></li>
        <li><%= button_to 'Log Out', destroy_user_session_path, method: :delete, class: 'btn btn-default navbar-btn' %></li>
      <% else %>
        <li><%= button_to 'Sign Up', new_user_registration_path, method: :get, class: 'btn btn-default navbar-btn' %></li>
        <li><%= button_to 'Log In', new_user_session_path, method: :get, class: 'btn btn-default navbar-btn' %></li>
      <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>